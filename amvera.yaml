meta:
  environment: python
  toolchain:
    name: pip
    version: 3.11

build:
  requirements: nexus_crm/requirements.txt

run:
  command: uvicorn nexus_crm.main:app --host 0.0.0.0 --port 80
